@if (TempData["Success"] is string success)
{
    <div class="toast show" role="status">@success</div>
}
@if (TempData["Error"] is string error)
{
    <div class="toast show error" role="alert">@error</div>
}
